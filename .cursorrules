本项目为企业级前端应用。

AI 必须严格遵守以下定义的所有架构规范、目录结构与编码约定，
禁止生成任何替代方案或自定义模式。

---

# 技术栈

框架：
- Next.js（App Router）
- React
- TypeScript

UI 相关：
- TailwindCSS
- shadcn/ui
- Lucide React

状态管理：
- Zustand

数据校验：
- Zod

网络请求：
- Axios

---

# 目录结构（严格遵守）

src/
  app/                 → 仅存放 Next.js 页面
  components/
    ui/                → 仅存放 shadcn 原生组件
    common/            → 通用公共组件
    business/          → 业务组件
  features/            → 业务功能模块
  store/               → Zustand 状态仓库
  hooks/               → 自定义 Hooks
  lib/                 → 工具函数、请求封装等
  types/               → 全局类型定义
  schemas/             → Zod 校验规则

禁止在 app 目录中编写任何业务逻辑。

---

# 页面规范

每个页面遵循结构：
page.tsx
loading.tsx（可选）
error.tsx（可选）

页面组件仅允许：
- 组合业务组件
- 调用自定义 Hooks
- 不编写任何业务逻辑

---

# 组件规范

所有组件必须：
- 为函数式组件
- 使用 TypeScript 完整类型定义
- 仅使用 TailwindCSS 样式
- 优先使用 shadcn/ui 组件

禁止编写类组件。

---

# 功能模块结构

以用户模块为例：
features/user/
  index.ts
  api.ts
  types.ts
  schema.ts
  store.ts
  components/
  hooks/

AI 必须严格按照此结构创建业务模块。

---

# 接口请求规范

所有接口必须：
- 使用封装后的 Axios 实例
- 统一定义在 features/*/api.ts 中
- 禁止在组件内直接调用 axios

错误示例：
useEffect(() => axios.get())

正确示例：
useUserList()

---

# Zustand 状态规范

Store 存放位置：
store/
或
features/*/store.ts

使用规则：
- 业务逻辑不得写在组件内
- 使用 selector 按需选取状态
- 禁止内联定义 store

---

# 表单规范

表单必须使用：
- react-hook-form
- zod
- shadcn 表单组件

结构约定：
schema.ts    校验规则
form.tsx     表单组件

---

# UI 规范

优先使用 shadcn 组件：
- Button
- Input
- Card
- Dialog
- Table
- Dropdown

图标：
仅允许使用 lucide-react 图标库。

---

# 样式规范

仅使用 TailwindCSS。

禁止：
- 编写独立 CSS 文件
- 使用 styled-components 等 CSS-in-JS
- 使用行内样式 style

---

# 表格规范

所有表格必须支持：
- 加载状态
- 空数据状态
- 分页
- 搜索（如业务需要）

统一使用 shadcn Table 组件。

---

# 弹窗规范

所有弹窗必须：
- 使用受控 open 状态
- 内部包含表单
- 提供提交 + 取消按钮

---

# 代码风格

必须遵守：
- 使用箭头函数
- 优先使用 const
- 禁止使用 any 类型
- Props 必须完整定义类型
- 页面使用 export default
- 组件使用具名导出

---

# 导入顺序

1. react 相关
2. next 相关
3. 第三方库
4. ui 组件
5. hooks
6. store
7. 类型文件

---

# 禁止行为

禁止：
- 重复创建功能相同的组件
- 在组件内直接编写 axios 请求
- 业务逻辑混杂
- 破坏既定目录结构
- 自定义新的 UI 体系

AI 必须严格执行以上所有规则。